# Ignore compiled files.

# Pattern Lab
pattern-lab

local.gulp-config.js

# We're not worried that PL runs exactly the same version on each local machine / CI build
composer.lock

# NPM
node_modules
npm-debug.log

# OS generated files #
######################
.DS_Store*
ehthumbs.db
!Icon
